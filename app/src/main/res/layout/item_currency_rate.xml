<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="currencyrateitem"
            type="me.maxdev.currencyconverter.ui.currencyconverter.CurrencyRateItem" />

        <variable
            name="vm"
            type="me.maxdev.currencyconverter.ui.currencyconverter.CurrencyConverterViewModel" />

        <import type="me.maxdev.currencyconverter.ui.currencyconverter.Converter" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{() -> vm.onItemClicked(currencyrateitem)}">

        <TextView
            android:id="@+id/label"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="@{currencyrateitem.name}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="EUR" />

        <EditText
            android:id="@+id/editText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ems="10"
            android:importantForAutofill="no"
            android:inputType="numberDecimal"
            android:onClick="@{() -> vm.onEditTextClicked(currencyrateitem)}"
            android:text="@{Converter.doubleToString(currencyrateitem.value)}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:on_focus_change="@{() -> vm.onEditTextClicked(currencyrateitem)}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
